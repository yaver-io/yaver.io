name: ${YAVER_TEST_IOS_SIM_DEVICE} local-first smoke
target: ios-sim
url: ${YAVER_TEST_IOS_SIM_DEVICE}
app: ${YAVER_TEST_IOS_APP}
requires_env:
  - YAVER_TEST_IOS_SIM_DEVICE
  - YAVER_TEST_IOS_APP
  - YAVER_TEST_IOS_BUNDLE_ID
steps:
  - goto: ${YAVER_TEST_IOS_BUNDLE_ID}
  - sleep_ms: 3000
  - screenshot: true

{
  "name": "yaver-gateway",
  "version": "0.0.0",
  "private": true,
  "description": "Yaver Premium captive-OpenRouter inference gateway (Cloudflare Worker)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.84.0"
  }
}

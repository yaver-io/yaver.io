# Todo Flutter — Yaver Serverless

Flutter Todo app backed by the Yaver Serverless Lite data API.

```bash
cd demo/yaver-serverless-todos/apps/flutter
flutter pub get
flutter run
```

Set the backend URL, project slug, and `pp_` token in the app.

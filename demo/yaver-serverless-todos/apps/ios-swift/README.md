# Todo iOS Swift — Yaver Serverless

SwiftUI Todo app backed by the Yaver Serverless Lite data API.

```bash
cd demo/yaver-serverless-todos/apps/ios-swift
xcodegen generate
open YaverServerlessTodo.xcodeproj
```

Set the backend URL, project slug, and `pp_` token in the app.

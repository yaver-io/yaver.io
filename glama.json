{
  "$schema": "https://glama.ai/mcp/schemas/server.json",
  "maintainers": ["kivanccakmak"],
  "environmentVariables": {
    "schema": {
      "type": "object",
      "properties": {
        "YAVER_HEADLESS": {
          "type": "string",
          "enum": ["0", "1"],
          "default": "0",
          "description": "Set to 1 to force Yaver's device-code OAuth flow instead of opening a browser."
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "placeholderArguments": ["YAVER_HEADLESS"]
  }
}

# Convex deployment URL (set automatically by `npx convex dev`)
# CONVEX_URL is typically set by the Convex CLI, not manually

# No additional env vars needed for the backend.
# OAuth credentials are configured in the web app runtime environment,
# not in the Convex backend directly.

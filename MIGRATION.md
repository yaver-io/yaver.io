# Migration Tracker Moved

`CLOUDFLARE.md` is now the single source of truth for the Vercel to Cloudflare migration.

This file intentionally stays as a pointer so older references do not leave two competing trackers in the repo.

Current canonical doc:

- `./CLOUDFLARE.md`

# Auto-generated by yaver services — do not edit manually
# Regenerated on: 2026-04-13T00:08:30+03:00

name: yaver-services

networks:
  yaver-net:
    driver: bridge

volumes:
  yaver-convex-data:
  yaver-minio-data:

services:
  minio:
    image: minio/minio
    restart: unless-stopped
    networks:
      - yaver-net
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_PASSWORD: "minioadmin"
      MINIO_ROOT_USER: "minioadmin"
    volumes:
      - yaver-minio-data:/data
    command: server /data --console-address :9001
  convex:
    image: ghcr.io/get-convex/convex-backend:latest
    restart: unless-stopped
    networks:
      - yaver-net
    ports:
      - "3210:3210"
      - "3211:3211"
    environment:
      INSTANCE_NAME: "yaver-local"
      CONVEX_SITE_URL: "http://127.0.0.1:3211"
    volumes:
      - yaver-convex-data:/convex/data
  convex-dashboard:
    image: ghcr.io/get-convex/convex-dashboard:latest
    restart: unless-stopped
    networks:
      - yaver-net
    ports:
      - "6791:6791"
    environment:
      NEXT_PUBLIC_DEPLOYMENT_URL: "http://127.0.0.1:3210"

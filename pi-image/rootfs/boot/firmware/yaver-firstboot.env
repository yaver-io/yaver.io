# Yaver Pi first-boot options
#
# Valid values:
#   daily   -> update Yaver and the dev stack every day
#   weekly  -> update Yaver and the dev stack every week
#   off     -> disable periodic updates
#
# First boot reads this file from the flashed image's boot partition.

YAVER_AUTO_UPDATE=daily

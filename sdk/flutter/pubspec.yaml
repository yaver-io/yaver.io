name: yaver
description: Yaver SDK — embed Yaver's local-first agent runtime into your Flutter and Dart apps. Create tasks, stream output, manage devices, and connect to developer workflows.
version: 0.2.2
homepage: https://yaver.io
repository: https://github.com/kivanccakmak/yaver.io/tree/main/sdk/flutter

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  http: ^1.2.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0

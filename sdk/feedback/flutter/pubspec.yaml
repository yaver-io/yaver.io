name: yaver_feedback
description: Visual feedback SDK for Yaver — shake-to-report, screenshots, voice annotations, and local-first developer workflows
version: 0.2.0
homepage: https://yaver.io
repository: https://github.com/kivanccakmak/yaver.io/tree/main/sdk/feedback/flutter

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
